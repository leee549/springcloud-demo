spring:
  cloud:
    config:
      uri: http://config-3344.com:3344
      name: springcloud-config-eureka-client #需要从GitHub上读取配置文件   不要后缀.yml
      profile: test        #本次访问的配置项
      label: master

